import { FutureFeatureScreen } from "@/screens/FutureFeatureScreen"

export default function Places() {
  return (
    <FutureFeatureScreen
      title="Places"
      description="Rooms, shelves, boxes, and nested storage places will be designed in a later product slice."
    />
  )
}
